[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
# Fix for "Failed to set permissions" error when using become_user with unprivileged users
# Required for tasks that switch to git user on Ubuntu/Debian systems
allow_world_readable_tmpfiles = True
